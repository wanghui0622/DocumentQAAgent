# FOLLOWUP 补充说明

---

## 1. 如果这套系统后续由你负责

### 已经可用、可以演示的部分

- **端到端闭环已跑通**：`parse → index → ask → eval`，CLI 与 Streamlit 均可操作。
- **国标扫描件路径相对成熟**：`src/pdf/pipeline.py` 在乱码文本层检测后会走 PaddleOCR + `structurer.py`，能产出条款块（如 3.1、3.2）和表 1 的 Markdown / 行级块，配合 `QueryRouter` 的 `clause_lookup` / `table_query` 加权，GBT 5 题 golden 在 `--force-ocr` 下基本可过。
- **拒答机制有明确分层**：`orchestrator.py` 在检索分数不足、超范围关键词（如花键）、Verifier 引用/数值/grounding 失败时会拒答，不是「硬编答案」。
- **有可复现的评测基线**：`tests/eval/golden_qa.json` 共 10 题，覆盖正文数值、表格、应拒答、跨文档拒答，`run_eval.py` 输出 Pass Rate 与 Retrieval Recall@5。

### 还不能放心交付的部分

| 区域 | 具体问题 |
|------|----------|
| **解析泛化** | `ClauseParser` 按页切分、不跨页合并；`TableParser` 依赖 GBT 特化关键词（AQL、尺寸公差等）；金融样本走 `text_native` 时整页一个 `paragraph`，无表格结构。 |
| **检索能力** | `HybridRetriever` 实际等同纯向量（`retriever.py`），无 BM25 或数字精确匹配；大数、表格单元格靠语义相似度，不稳定。 |
| **外部依赖** | Embedding 与 LLM 均为硬依赖，无 Key 无法建索引；OCR 每页 2–3 分钟，客户批量文档成本高。 |
| **质量保障** | 仓库内无 `test_*.py` 单元测试；`eval` 每次调真实 API，结果随模型波动；`sample_blocks.json` 表 1 AQL 为 1.5，与 `golden_qa_gbt.json` GBT-Q3 期望 1.0 不一致，说明回归链路有漏洞。 |
| **文档与实现不一致** | UI 写「混合检索」、`.env.example` 写 BM25 降级，代码均未实现。 |

### 下一步优先处理什么（及原因）

**P0：解析质量可视化 + golden 回归固化**

1. 对每份入库文档跑 `python3 -m src.cli blocks --doc-id <id>`，建立「条款数 / 表格数 / 空块率」基线，人工 spot-check 3–5 页。
2. 修复 `sample_blocks.json` 与 golden 的不一致；补充不依赖 LLM 的 pytest（`extract_clause_id`、router 分类、引用 fuzzy match）。
3. 将 `eval --suite all` 纳入 CI nightly（有 Key）+ PR 跑解析/路由单测（无 Key）。

**原因**：检索和生成再优化，也救不了「块切错」；且当前缺少自动化回归，改一处容易 silently break。

**P1：金融文档表格解析 + 检索增强**

- 为 `text_native` 路径增加表格检测（PyMuPDF `find_tables()` 或版面分析），否则 AGENT-Q1~Q5 完全赌向量语义。
- 实现真正的 hybrid（BM25 + 向量）或对 `table_query` 增加数字/列名精确匹配层。

**原因**：作业第二份文档是财报数值场景，这是客户最可能追问「大数准不准」的地方，也是当前效果最不稳的短板。

---

## 2. 如果要给客户试用

### 上线前会和产品 / 客户 / 技术负责人确认的事项

| 维度 | 具体确认点 |
|------|------------|
| **目标** | 试用要解决什么：合规条款查询？财报指标问答？还是通用文档助手？默认「只答文档内、带引用、不确定就拒答」是否接受。 |
| **文档边界** | 文档类型（扫描 / 文本层）、数量、页数、是否含复杂跨页表格；是否允许 `--force-ocr` 及可接受的解析耗时（当前约 2–3 分钟/页）。 |
| **基础设施** | Embedding 与 LLM 端点（是否用客户自有模型）、API 配额、数据是否出域；Chroma 持久化路径与备份策略。 |
| **验收标准** | 用 golden 10 题还是客户自有 20–30 题；Pass Rate / 拒答准确率 / 引用准确率各多少算合格；允许多少「拒答过多」作为 trade-off。 |
| **第一版范围** | 明确 IN：单文档或指定 doc_id 范围问答、带页码引用、表格/条款类问题。明确 OUT：跨文档推理、文档外知识、实时更新、多轮复杂推理。 |
| **风险告知** | OCR 启发式对版式敏感；纯向量对大数可能不稳；模型/API 变更会导致 eval 波动——需约定模型版本锁定。 |

### 如何理解目标与交付

我会用「**可验收的最小闭环**」对齐：第一版 = 客户指定的 1–2 类文档能完成「上传 → 解析可见 → 10 个样例问题有引用地答对或正确拒答」。超出范围的（如全库自动泛化、零拒答率）写入 backlog，避免 demo 范围膨胀导致交付失控。

---

## 3. 如果要保证质量和稳定性

### 如何验证系统可靠

**三层验证**：

1. **解析层**：`blocks` 输出与 PDF 原文对照（条款编号、表头、关键数值）；统计 block 类型分布，异常（如整本文档只有 paragraph）立即告警。
2. **检索层**：golden 集的 Retrieval Recall@5；对 table/clause 类问题检查 top-5 是否含 `gold_pages` 对应内容。
3. **端到端**：`python3 -m src.cli eval --suite all`，记录 Overall Pass、Answer/Citation/Refusal Accuracy；同一模型连跑 3 次看波动。

**发布前 checklist**：

- [ ] `docs` 显示目标文档 `parsed=true, indexed=true`
- [ ] golden 10 题 Pass ≥ 约定阈值（如 8/10）
- [ ] 手动抽 5 个客户真实问题，检查引用页码可点开核对
- [ ] 无 Key / 超时 / 空检索 的降级提示清晰，不 silent fail

### 演示或生产前出问题的排查与止损

| 现象 | 排查路径 | 止损 | 确认修复 |
|------|----------|------|----------|
| 答案胡编 / 数值错 | 看 citations 是否匹配 evidence；查 `verifier.py` checks_failed | 临时调高 `VERIFIER_MIN_GROUNDING` 或降低 temperature；不行则拒答 | golden + 客户 bad case 重跑 |
| 大量拒答 | 看 `REFUSE_*` 前缀（检索阈值 / 超范围 / verifier） | 调低 `RETRIEVAL_SCORE_THRESHOLD`（需记录风险） | 对比拒答前后 Pass Rate |
| 检索不到 | `blocks.json` 是否含目标条款/表行；chunk 是否过长被截断 | 对问题文档 `--force-ocr` 重解析 | Recall@5 回升 + 人工 spot-check |
| API 失败 | 日志中 embedder / generator 报错 | 切换备用端点；演示改用 `seed` 预建索引 | 冒烟 `ask` 单题 |

原则：**先止损保演示不翻车（宁可多拒答），再根因修复，最后用 eval + spot-check 签字放行。**

---

## 4. 如果时间有限

### 优先级安排

| 优先级 | 事项 | 时间盒 |
|--------|------|--------|
| P0 | golden eval 全绿或达约定阈值；阻塞性 bug（无法 index/ask） | 第 1 天 |
| P1 | 客户演示涉及的文档解析 spot-check；修复明显 block 错误 | 第 2 天 |
| P2 | 检索/Verifier 参数扫一遍；UI 文案与实现一致 | 有余力再做 |
| Defer | BM25 真 hybrid、跨页结构、插件化 Parser | 试用后迭代 |

### 风险同步与资源协调

- **每日 15 分钟站会**：Pass Rate、拒答率、解析耗时、API 配额，用红黄绿标风险。
- **提前 48 小时冻结 scope**：演示问题清单锁定，新需求进 v2。
- **分工**：我负责解析 + eval + 参数；前端/产品负责演示脚本与客户问题预演；运维确认 API Key 与网络。

### 关键目标保障

若时间只够一件事：**保证演示文档的 golden + 客户预演问题 100% 有可追溯引用或正确拒答**。宁可缩小文档范围，也不扩到未验证的版式。

---

## 5. 你会如何使用 AI 或智能体工具


| 交给 AI / Agent | 必须自己把关 |
|-----------------|--------------|
| 模块骨架、Typer/Streamlit 样板代码 | 架构是否符合「Evidence-only + 拒答」原则 |
| Prompt 初稿与迭代 | golden_qa 回归；拒答是否误伤/漏伤 |
| OCR 后处理启发式（乱码检测、行聚类参数） | 对照 `blocks.json` 与 PDF 原文 |
| 评测题与 `expected_keywords` 初稿 | 按标准正文核对数值（如 AQL 1.0 vs 1.5） |
| 日志解读、bad case 归类、修复方案草稿 | 是否引入新回归；是否 over-fit 单文档 |
| 文档/README 撰写 | 交付边界、已知限制是否写清楚 |

**可信度检查**：AI 产出一律过「**可自动验证**」门槛——能写 pytest 的写 pytest，能跑 eval 的跑 eval，不能只靠「读起来合理」。对解析类改动，必看 diff 后的 `blocks` 样例页。

---

## 6. 补充一个真实例子

这是我参与的**第一个 AI 项目**——为某零售企业建设**智能客服问答系统**，目标是在在线客服入口承接高频咨询（物流、退换货、会员权益等），降低人工坐席压力，同时保证合规话术、不乱答。

### 调研：先搞清楚「值不值得做、从哪切入」

项目启动前，我花了约两周做调研，避免一上来就堆模型：

1. **拉历史工单数据**：与业务部门一起导出近 6 个月在线客服记录，按意图聚类，发现 Top 20 问题占咨询量约 65%，且答案高度依赖内部政策文档（退换货规则、物流时效表、会员等级说明）。
2. **量化人工痛点**：统计平均响应时长、一次解决率、转人工率；明确智能客服要优化的核心指标是「**高频标准问一次解决**」，而不是一上来替代复杂投诉处理。
3. **技术路线比选**：对比规则引擎、检索增强（RAG）、端到端生成三种方案。结论：政策类问答适合 **RAG + 拒答/转人工**，生成类需要强约束，否则合规风险高。
4. **输出调研结论**：建议第一期只做「会员 + 物流」两条业务线、约 80 个标准问法、接入企业微信客服入口；复杂纠纷和情绪化投诉一律转人工。这份结论成为后续和 AI 部、业务部对齐的基线。

### 与 AI 部门协作：分工清晰，共用评测语言

AI 部门负责模型底座和推理平台，我作为项目侧算法负责人，负责效果链路和验收。协作方式：

| 分工 | AI 部门 | 我（业务侧算法） |
|------|---------|------------------|
| 模型 | 提供 LLM、Embedding 接口与配额 | 选型评估、Prompt / 检索策略 |
| 数据 | 通用安全过滤、日志基建 | 知识库清洗、chunk 策略、golden 集 |
| 评测 | 平台监控、延迟与可用性 | 准确率、拒答率、转人工率等业务指标 |

**关键协作机制**：

- **双周例会**：同步 bad case，避免「平台觉得模型没问题、业务觉得完全不能用」。
- **共建 golden 集**：从真实工单抽 200 条，业务专家标标准答案和「必须转人工」标签，我负责跑批评测，AI 部协助调模型参数（temperature、max tokens）。
- **一次较大分歧**：业务希望「尽量多答」提升拦截率，AI 部倾向保守。我拉了 50 条高风险题（退款金额、时效承诺）做对比实验，数据显示放开后误答率上升 18%。最后三方确认：**宁可多转人工，也不能错答政策**——这和本次作业里 Evidence-only + Verifier 拒答的思路一致。

### 与业务部门协作：把「能不能上线」变成可验收条款

业务部不是「提需求的人」，而是**验收共建方**：

1. **第一期范围对齐**：一起确认 80 个标准问、禁答清单（价格争议、未发布活动、跨订单纠纷），写入验收文档。
2. **知识来源共建**：业务提供政策 PDF、FAQ 表格、客服话术库；我负责解析入库，每周发「知识覆盖报告」——哪些问题检索不到、哪份文档过期。
3. **客服主管参与标注**：每条 golden 答案必须经过一线主管确认，避免「技术上正确但客服不会这么说」。
4. **灰度陪跑**：先内部员工试用 1 周，再开 5% 流量，客服主管每天看 20 条对话抽检，我根据反馈当天修检索或补知识。

### 推进与结果

项目周期约 3 个月。我每周维护一张风险看板：解析覆盖率、检索命中率、答对率、误答率、转人工率。上线前最后两周集中攻坚 bad case，把误答类从 23 条压到 6 条，其余通过「明确转人工话术」兜底。

**最终结果**：

- 按期上线会员 + 物流两条线，灰度两周后全量。
- 高频标准问**一次解决率 72%**（目标 65%），**转人工率下降约 30%**，合规类误答经抽检为 0。
- 沉淀了 golden 评测集、周报模板和知识更新流程，二期扩到售后投诉分类时直接复用。

**我的收获**：第一个 AI 项目让我意识到，效果优化不只是调模型——**调研定边界、和 AI 部用数据对齐技术方案、和业务部用 golden 集对齐验收**，三件事做扎实了，项目才能按期交付。这次作业里的 `golden_qa`、拒答机制、blocks 人工 spot-check，本质上就是当时那套方法的缩小版。

---

## 7. 如果文档章节层级解析不对

### 定位问题

1. **抽样对比**：`python3 -m src.cli blocks --doc-id <id>` 导出 blocks，按 `clause_id` / `page` / `block_type` 排序，与 PDF 同页对照。
2. **分层归因**：
   - OCR 行切分错 → 看 `ocr_engine.py` 原始行与 `group_lines_by_row` 聚类结果
   - 条款编号漏识 → `extract_clause_id()` 正则是否匹配「3.1」「3.1.1」及 OCR 噪声
   - 表格误入正文 / 反之 → `LayoutParser` 的 `in_table` 状态机（进入后不退出的问题）
   - 跨页断裂 → `ClauseParser` 按页独立处理，续条款在页边界被截断
3. **影响范围**：统计错误 block 占比；对 golden 题跑 Retrieval Recall@5，看是否 `gold_pages` 进不了 top-5；按文档类型（国标 / 财报）分组，判断是系统性还是单文档版式问题。

### 修复方案

| 层级 | 做法 |
|------|------|
| **短期** | 问题文档 `--force-ocr`；手工修正 `blocks.json` 中明显错块后 `index --skip-parse`；TableParser 补充该客户表格关键词 |
| **中期** | 跨页 clause 合并（相邻页同 `clause_id` 续写拼接）；表格状态机支持退出；章节标题（「1 范围」）单独 block 类型 |
| **长期** | 按 `plugins/base.py` 注册文档类型 Parser（国标 / 财报 / 合同）；引入版面分析模型；块级人工纠错反馈回路 |

### 验证修复有效

- 解析指标：条款数、表格行数与人工标注一致率
- 检索指标：golden Recall@5 不低于修复前基线
- 端到端：eval Pass Rate 提升且不误伤拒答题（GBT-Q4、AGENT-Q4）
- 回归：新增该版式 blocks fixture 进 pytest，防止回退

### 短期交付止损

若来不及改 Parser：**限定支持版式清单** + 对不在清单内的文档提示「需人工校对 blocks」；演示只使用已验证文档；问答侧对低分检索统一拒答，避免错引。

---

## 8. 如果系统效果不稳定

### 选定问题：金融文档（text_native）表格数值检索不稳

**为何选它**：`pipeline.py` 的 `TextExtractor` 对 `agent开发作业样本` 每页产出一个 `paragraph`，`clause_id=None`，无行列结构。AGENT-Q1~Q3 问的是财报大数（营业收入、净利润等），纯靠 `VectorRetriever` 语义相似度 + LLM 抽取，同一问题不同运行可能命中不同页码片段，Verifier 数值校验有时通过有时拒答。

### 评价标准

| 指标 | 定义 |
|------|------|
| **Table QA Accuracy** | `golden_qa_agent_sample.json` 5 题中 `expect_answered=true` 的题，关键词全匹配且 citations 非空 |
| **Numeric Exact Match** | 答案中数字与 evidence 中数字集合一致（沿用 `verifier.py` 逻辑） |
| **Retrieval Recall@5** | top-5 是否覆盖 `gold_pages` |
| **稳定性** | 同一模型连跑 5 次，Pass 率标准差 < 10% |

### 收集 bad case

- 跑 `eval --suite agent` 导出失败题 id、检索片段、拒答原因
- 在 Streamlit 用客户常问的 10 个变体表述（「营收」「营业收入」「合并营收」）记录是否一致
- 建 `bad_cases/agent_sample.json`：`question / retrieved_chunks / answer / expected / root_cause`

### 定位原因

1. **解析**：blocks 无表格 → 长段落 embedding 稀释关键数字
2. **检索**：`table_query` 加权对无 table block 无效；top_k 内混入无关页
3. **生成**：LLM 从多段 evidence 抽错列
4. **校验**：`fuzzy_contains` 阈值 0.6 可能放过错误引用

用二分法：固定 retrieval 结果手动喂 generator，若仍错 → 生成问题；若 retrieval 不含 gold_page → 解析/索引问题。

### 优化尝试（按性价比）

1. PyMuPDF `find_tables()` 为金融 PDF 产 `table` block + 行级 chunk
2. 对含数字的问题走关键词 / 正则预过滤 chunk（如含「营业收入」且含「万元」）
3. `table_query` 时 `RETRIEVAL_FINAL_K` 提高到 8，观察 Recall
4. Generator prompt 强调「只取与问题列名同行的数值」

### 判断优化是否有效

- **主指标**：agent 套件 Pass Rate 从基线提升 ≥ 20%，且 5 次运行 Pass ≥ 4/5
- **对照实验**：只开一项改动跑 eval，避免多变量同时改无法归因
- **失败即回滚**：Pass 率下降或拒答率异常升高则撤回该改动

若短期无法改 Parser：**交付说明中写明「金融表格类问题建议人工核对引用页」**，并对低 grounding 分强制拒答。

---

## 工作方式补充

### 1. 遇到不清楚但又比较急的需求

**先确认三件事**：

1. **长什么样**：给一个具体例子——「用户问 X，期望答 Y 并引用 Z 页」。
2. **不能做什么**：是否允许拒答、是否允许查文档外知识。
3. **时间与资源**：几天、能否动解析/OCR、有无 API 预算。

**推进第一版**：用现有能力做「窄而深」的切片——例如只支持 1 份 PDF + 5 个演示问题，端到端跑通再扩。每天结束前发 5 分钟演示录屏或 eval 截图，让对方尽早纠偏。

**避免方向做偏**：把「做的 / 不做的 / 待确认的」写进一条消息或 README 小节，请对方文字确认；未确认项默认不做。

### 2. 关键节点前发现问题

周五晚上发现周一演示会受影响的问题，我会：

1. **30 分钟内定性**：阻塞演示（无法启动 / 必答题答错）vs 体验问题（慢、偶发拒答）。
2. **同步风险（当晚）**：告知负责人：现象、影响范围、两个方案（修 / 绕）、我倾向哪个；确认周一演示范围是否可缩。
3. **优先级**：演示脚本涉及的 doc_id + 问题列表 > golden 全量 > 其他优化。
4. **个人投入**：阻塞性问题我通宵盯到「有明确回退方案」——例如修不好 Parser 就换预置 `seed` 索引 + 手工 blocks，保证演示路径可用；非阻塞问题记录 issue，演示时口头说明已知限制。
5. **周一早上**：留 1 小时缓冲，只跑演示脚本不复盘大改。

### 3. 沟通中出现分歧

做法：

- **先对齐事实**：用 blocks 截图、eval 结果、日志把「我觉得」变成「数据显示」。
- **分离决策与时间盒**：需求范围是否可以缩小；技术分歧大的放会后异步调研，不占会议。
- **给选项而非辩论**：A 全 OCR 准但慢；B text_native 快但财报表不稳，让产品选 trade-off。
- **书面纪要**：结论、负责人、截止时间一行话跟进，避免反复拉扯。

对方着急时，我先承认进度压力，再明确「我能保证的最小结果」和「需要延期才能做的」，不空口承诺。

### 4. 多件事情同时压过来

**安排方式**（按紧急且重要矩阵）：

| 时段 | 事项 |
|------|------|
| 上午专注块 | 开发 / 修复（免打扰） |
| 下午碎片 | 排查、回复沟通 |
| 傍晚固定 | 跑 eval、更新风险看板 |

并行时：**一个主线程**（当前 sprint 目标），排查类设时间盒（如 1 小时未定位则升级或绕路）。

**AI / Agent 提速**：

- 用 AI 生成 pytest 样板、日志摘要、bad case 表格
- 用 Agent 批量跑 `blocks` 对比、整理 eval diff

**一定自己复查**：golden 的 `expected_keywords`、涉及拒答逻辑的改动、给客户看的演示脚本、任何数值类答案是否可追溯到 evidence 页码。

### 5. 讲一个真实例子

智能客服项目**上线前一周**，业务部突然提出要给客服中心总监的汇报演示，希望展示「退换货政策问答」能力。联调时，我发现新版退换货政策 PDF 入库后，检索经常命中旧版条款，演示题「七天无理由退货是否含拆封商品」会答成过时规则——**演示必挂**。

**判断**：全量重洗知识库和重新解析来不及；演示只涉及退换货 5 个固定问题，可以针对性止损，不影响整体上线节奏。

**沟通**：

- **当晚同步业务负责人**：说明风险，建议演示范围缩为「已人工校验的退换货知识包 + 5 个预演问题」，旧版文档标注下线，会上如实说明「其余品类下周接入」。
- **同步 AI 部接口人**：请他们协助锁定 Embedding 模型版本，避免周末平台升级导致检索漂移；对方 30 分钟内给了冻结方案。

**推进**：

- 与业务专家连夜核对退换货 golden 5 题，手工修正知识库中 3 条过期 chunk。
- 对演示问题开启更严格的置信度阈值，不确定时统一回复「为您转接人工客服」——业务主管起初有异议，我用 20 条高风险样例证明「错答比转人工代价更大」，最终同意。
- 拉业务部客服主管预演，换掉一个表述刁钻的问法，避免口语变体检索失败。

**结果**：汇报通过，总监认可「答得上且有依据、不敢乱答」；项目按原计划周三灰度上线。事后把「政策文档版本号 + 生效日期」写进知识入库必填字段，避免同类问题再发生。

教训：**跨 AI 部、业务部协作时，提前冻结演示路径和知识版本，比赌全量修复更可靠。**

